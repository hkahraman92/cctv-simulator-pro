# Runtime dependencies for the CCTV simulator
ttkbootstrap>=1.10
customtkinter>=5.2
pillow>=10.0
numpy>=1.24
openpyxl>=3.1
reportlab>=4.0

# Optional: real GeoTIFF / DEM file loading in terrain_loader.load_geotiff_or_dem().
# Without it the app falls back to reading the file as a greyscale heightmap,
# whose values are relative, not metres. Online DEM download does not need it.
# rasterio>=1.3

# Build only
pyinstaller>=6.3
